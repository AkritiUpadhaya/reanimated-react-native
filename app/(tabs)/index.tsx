import Animated from 'react-native-reanimated';

export default function HomeScreen() {
  return (
    <Animated.View
      style={{
        width: 100,
        height: 100,
        backgroundColor: 'red',
      }}
    />
  );
}